# API Gateway

## `infra/`

- `compose.yaml`, `Innowise-Platform.postman_collection.json` — platform-wide Docker Compose file and Postman collection.
- `k8s/` — manifests and scripts to deploy the whole platform to a local `kind` Kubernetes cluster.
