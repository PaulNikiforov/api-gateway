# Downstream service URLs — resolved via innowise-platform Docker network by default.
# Override here only if the services run outside that network.
AUTH_SERVICE_URL=http://auth-service:8081
USER_SERVICE_URL=http://user-service:8080
ORDER_SERVICE_URL=http://orderservice:8082
